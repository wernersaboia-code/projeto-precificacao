# No .github/workflows/jira.yml (opcional):
name: Jira Integration
on:
  push:
    branches: [main]
jobs:
  update-jira:
    runs-on: ubuntu-latest
    steps:
      - uses: atlassian/gajira-login@v3
      - uses: atlassian/gajira-transition@v3
